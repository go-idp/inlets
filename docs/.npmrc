# Mermaid + Vite dev: pnpm’s strict node_modules can pull `dayjs.min.js` without a
# proper ESM default. Hoisting matches npm/yarn layout and avoids the runtime error.
# See https://github.com/emersonbottero/vitepress-plugin-mermaid/issues/47
shamefully-hoist=true
